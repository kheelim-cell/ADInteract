# Deployment is handled by Vercel (auto-deploys on push to main).
# This workflow is kept as a no-op placeholder to avoid GitHub Actions errors.
name: Deploy (Disabled — Vercel handles this)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by Vercel. Nothing to do here."
