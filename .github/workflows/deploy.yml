# GitHub Pages deployment — DISABLED after migrating to Vercel.
# Vercel auto-deploys from the main branch. This file is kept for reference only.
# To re-enable, uncomment the triggers and update the adapter in svelte.config.js.

# name: Deploy
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
